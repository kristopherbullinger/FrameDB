<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><% if (locals.character) { %>
          <%= `${character.displayName}` %>
          <% } else { %>
          <%=  "Tekken7Database" %>
          <% } %>
    </title>
    <link rel="stylesheet" href="/css/main.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
  </head>
  <body>
